<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title || "ENI - Gestion des Élèves-Instituteurs" %></title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

  <!-- Custom CSS (facultatif) -->
  <link rel="stylesheet" href="/css/style.css">

  <!-- Favicons -->
  <link rel="icon" href="/favicon.ico">
</head>
<body>

  <!-- Barre de navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">ENI Abomey</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarContent">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/eleves">Élèves</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/bibliotheque">Bibliothèque</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/comptabilite">Comptabilité</a>
          </li>
          <!-- Ajoute d'autres liens si nécessaire -->
        </ul>
      </div>
    </div>
  </nav>

  <!-- Contenu principal -->
  <main class="container my-4">
    <% if (typeof success !== 'undefined') { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <!-- Contenu dynamique -->
    <%- body %>
  </main>

  <!-- Pied de page -->
  <footer class="bg-light text-center py-3 mt-auto">
    <small>© <%= new Date().getFullYear() %> ENI Abomey. Tous droits réservés.</small>
  </footer>

  <!-- Bootstrap JS (popper + js) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
